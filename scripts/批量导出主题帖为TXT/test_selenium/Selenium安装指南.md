# Selenium 安装指南（超简单版）

## 📋 安装步骤

### 第1步：安装Selenium

打开命令提示符（或终端），运行：

```bash
pip install selenium
```

等待安装完成（约1分钟）。

---

### 第2步：下载浏览器驱动

Selenium需要一个驱动来控制浏览器。**选择您常用的浏览器：**

#### 选项A：Chrome（推荐）

1. **检查Chrome版本**
   - 打开Chrome
   - 地址栏输入：`chrome://settings/help`
   - 记下版本号（如：120.0.6099.129）

2. **下载ChromeDriver**
   - 访问：https://googlechromelabs.github.io/chrome-for-testing/
   - 找到与您Chrome版本匹配的驱动
   - 下载对应系统的版本（Windows/Mac/Linux）
   - 解压得到 `chromedriver.exe`（Windows）或 `chromedriver`（Mac/Linux）

3. **放置驱动文件**
   - 把 `chromedriver.exe` 放在脚本同一个文件夹
   - 或者放在系统PATH中（如 `C:\Windows\System32\`）

#### 选项B：Edge

1. **检查Edge版本**
   - 打开Edge
   - 地址栏输入：`edge://settings/help`
   - 记下版本号

2. **下载Edge驱动**
   - 访问：https://developer.microsoft.com/en-us/microsoft-edge/tools/webdriver/
   - 下载对应版本
   - 解压得到 `msedgedriver.exe`

3. **放置驱动文件**
   - 放在脚本同一个文件夹

#### 选项C：Firefox

```bash
pip install webdriver-manager
```

这个包会自动管理Firefox驱动，无需手动下载。

---

### 第3步：验证安装

运行测试脚本 `test_selenium.py`（我会创建）：

```bash
python test_selenium.py
```

如果浏览器自动打开并显示百度，说明安装成功！

---

## 🚨 常见问题

### Q1: 提示找不到chromedriver？

**解决方法：**
- 确保 `chromedriver.exe` 在脚本同一文件夹
- 或者在脚本中指定完整路径

### Q2: 版本不匹配？

**解决方法：**
- ChromeDriver版本必须与Chrome版本匹配
- 重新下载对应版本的驱动

### Q3: pip install失败？

**解决方法：**
```bash
pip install selenium --upgrade
```

或者使用国内镜像：
```bash
pip install selenium -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q4: Mac提示"无法验证开发者"？

**解决方法：**
```bash
xattr -d com.apple.quarantine chromedriver
```

---

## ✅ 完成检查清单

- [ ] 已安装 Selenium (`pip install selenium`)
- [ ] 已下载浏览器驱动（chromedriver/msedgedriver/geckodriver）
- [ ] 驱动文件已放在正确位置
- [ ] 运行测试脚本成功

完成后，就可以运行主脚本了！

---

## 💡 小贴士

**如果您不想安装驱动：**

使用 `webdriver-manager` 自动管理：

```bash
pip install webdriver-manager
```

然后脚本会自动下载和管理驱动，无需手动操作。
